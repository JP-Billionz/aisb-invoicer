# Status — Day 1 (Phase 1A)

**Date:** 2026-05-17
**Author:** Claude Code (claude-opus-4-7)
**Repo:** https://github.com/JP-Billionz/aisb-invoicer
**Staging URL:** https://aisb-invoicer.onrender.com
**Render service:** `srv-d84ssvgjo89c73bspq70` (Free Web Service)
**Database:** Neon free Postgres — AWS US-East-1
**Deployed commit:** `77d6ee8` (baseUrl fix on top of `44a4222`)
**Deploy status:** ✅ Verified green on Render after the `tsconfig.json`
`baseUrl` fix — `GET /` returns 200, build healthy.

## TL;DR

Phase 1A is shipped and live. Repo at
[github.com/JP-Billionz/aisb-invoicer](https://github.com/JP-Billionz/aisb-invoicer),
`main` at `77d6ee8`, Render Free Web Service deployed against Neon Free
Postgres, staging URL returning 200. The whole Phase 1A stack runs on a
no-card-on-file footprint — paid tiers come in Phase 1B alongside the
first founding customer.

## What's live on staging

End-to-end on https://aisb-invoicer.onrender.com:

- **Marketing landing** (`/`) — brand-correct dark theme, single CTA into signup
- **Email magic-link signup** (`/signin`) — Auth.js v5 + Prisma adapter; in
  Phase 1A magic links print to the Render service logs (no SendGrid yet),
  so signup today is "request link → tail logs → paste URL into browser"
- **Auto tenant creation** on first sign-in — new `Tenant` row with
  `nextInvoiceNumber = 10027`, `trialEndsAt = now + 14 days`,
  `subscriptionStatus = 'trialing'`
- **Dashboard shell** (`/invoices`) — auth-gated, trial countdown banner,
  most-recent-25 invoice history
- **Create invoice** (`/invoices/new`) — client form with live subtotal/VAT/
  total math; submit → atomic `$transaction` increments the per-tenant
  counter and inserts the row; bill-from address snapshotted on the
  invoice at create time
- **Server-side PDF export** (`/api/invoices/:id/pdf`) — jsPDF +
  jspdf-autotable, V.1 layout (lavender header → green-purple-green
  accent strip → items table → totals block → "Powered by AI Solutions
  Barbados" footer)
- **Soft-delete API** (`DELETE /api/invoices/:id` sets `deletedAt`; no
  UI wired yet, that's Phase 1B item 6)
- **Read-only settings page** (`/settings`) — shows tenant business
  details; editable form is Phase 1B item 5
- **Health check** (`/api/health`) returns `{ ok: true }` for uptime monitors
- **Tenant isolation enforced** through `withTenant({ db, tenantId, userId })`
  in `src/lib/tenant.ts` — every invoice API route and dashboard page
  uses it. Direct `prisma.invoice.find*` outside `src/lib/` is forbidden.

## What works (verified locally)

- ✅ Next.js 14 App Router + TypeScript strict — `tsc --noEmit` clean
- ✅ Tailwind dark theme with brand palette (#7C3AED purple, #39D353 green)
- ✅ Prisma schema matches `docs/03-DATA-MODEL.md` exactly
  (Tenant, User, Invoice + NextAuth Account/Session/VerificationToken,
  `[tenantId, number]` unique, soft-delete via `deletedAt`)
- ✅ NextAuth (Auth.js v5) email magic-link, Prisma adapter, database sessions
- ✅ `events.createUser` callback creates a Tenant on first signup, sets
  `nextInvoiceNumber = 10027` and `trialEndsAt = +14 days`
- ✅ Tenant isolation enforced through `withTenant({ db, tenantId, userId })`
  in `src/lib/tenant.ts` — every invoice API route and dashboard page
  uses it
- ✅ `POST /api/invoices` increments the per-tenant counter and inserts
  the invoice in a single `$transaction` (Rule 3 from the data-model spec)
- ✅ Bill-from snapshotted on Invoice at create time (Rule 4)
- ✅ Soft-delete only (`DELETE /api/invoices/:id` sets `deletedAt`)
- ✅ Server-side jsPDF render with the V.1 design language:
  lavender header band, green-purple-green accent strip, branded
  totals block, "Powered by AI Solutions Barbados" footer
- ✅ Marketing page, sign-in page, check-email page, dashboard layout
  (with trial countdown banner), invoices list, new-invoice form,
  read-only settings page
- ✅ `/api/health` returns `{ ok: true }` for Render's health check
- ✅ Validation via Zod (`createInvoiceSchema` — rejects 0-item invoices
  and missing customer name per spec edge cases)
- ✅ `next build` produces 12 routes, no errors, no warnings
- ✅ Initial Prisma migration committed at
  `prisma/migrations/20260517000000_init/migration.sql` — the build
  runs `prisma migrate deploy` (idempotent, skips already-applied)
- ✅ `render.yaml` Blueprint provisions the free-tier Web Service;
  `DATABASE_URL` is a manual env var pointing at an external Neon
  free-tier Postgres (Render removed free Postgres in 2024).
  `NEXTAUTH_SECRET` is auto-generated by the Blueprint.

## Phase 1A — done

Deploy landed at `77d6ee8` (baseUrl fix on top of `44a4222`) on Render
service `srv-d84ssvgjo89c73bspq70`, backed by Neon free Postgres in AWS
US-East-1. Staging URL returns 200 ✅.

Single fix needed after the initial push: `tsconfig.json` was missing
`"baseUrl": "."`, which broke Next 14's `@/*` path alias resolution at
build time. One-line patch, no source changes. Full deploy click-through
preserved in [`DEPLOY-CHECKLIST.md`](../DEPLOY-CHECKLIST.md) for future
environments.

## Phase 1B scope (next session)

The five tracks from Jamai's brief, in the order they unblock revenue:

### 1. SendGrid live magic-link email
Phase 1A logs magic-link URLs to Render service logs. Founding-customer
onboarding needs real inbox delivery.
- Provision SendGrid free tier (100 sends/day), create API key with
  `Mail Send` scope only.
- Authenticate `aisolutionsbb.com` as a sending domain (3 CNAME records
  in Cloudflare).
- Paste `SENDGRID_API_KEY` + `EMAIL_FROM` into Render env vars; the
  branch in `src/lib/email.ts` is already written and gated on the key.
- Smoke test: request magic link → receive in inbox → click → land in
  dashboard authenticated.

### 2. Stripe live mode + billing
Founding customer cannot pay until this is wired.
- Create three products in Stripe live mode: Starter / Growth / Pro
  at BBD $29 / $59 / $99 (currency confirmed with Jamai — see decision
  log). Capture price IDs.
- `POST /api/stripe/checkout` → create Checkout session for the
  authenticated tenant, redirect to Stripe Hosted Checkout.
- `POST /api/stripe/webhook` with `STRIPE_WEBHOOK_SECRET` signature
  verification → updates `Tenant.subscriptionStatus`,
  `subscriptionPlan`, `stripeCustomerId`, `currentPeriodEnd`.
- Daily Render Cron Job: any `Tenant` with
  `trialing` + `trialEndsAt < now()` → downgrade to `past_due` and
  block invoice creation in `withTenant`.
- Customer portal link from `/settings` for self-serve plan changes
  and card updates.

### 3. Custom domain — `invoices.aisolutionsbb.com`
Needs to land before Stripe webhooks go live so the webhook URL is
stable from day one.
- CNAME `invoices` → Render-assigned hostname in Cloudflare.
- Add the custom domain to the Render Web Service; wait for the
  Let's Encrypt cert to issue.
- Update `NEXTAUTH_URL` env var to `https://invoices.aisolutionsbb.com`.
- Add a 301 redirect from `aisb-invoicer.onrender.com` (Render setting).

### 4. Upgrade to paid Render + Postgres tiers
Free tiers cold-start after 15 min and Neon free DB pauses — neither is
acceptable for a paying customer.
- **Render Web Service:** Free → Starter ($7/mo, always-on, no spin-down).
- **Postgres:** decide between
  - Neon Launch ($19/mo, autoscale, point-in-time recovery), or
  - Render Basic-256mb ($7/mo, simpler ops, same provider as the app).
  Recommendation: Render Basic-256mb to start — lower cost, one less
  vendor, migrate to Neon Launch only if we hit the 256mb ceiling.
- Migration plan: `pg_dump` from Neon free → `pg_restore` into new DB
  → swap `DATABASE_URL` in a single Render deploy. ~5 min downtime
  window, schedule outside business hours.

### 5. First founding customer onboarding
The forcing function for items 1–4. Plan:
- Pre-flight: items 1–4 done and verified green.
- Create the tenant manually (or via signup) and seed their business
  details + logo via the settings editor (Phase 1B item 5b below).
- Walk them through creating their first real invoice on a screen share.
- Hand off Stripe Checkout link for the Starter plan; verify webhook
  flips `subscriptionStatus` to `active`.
- Set up Better Stack monitoring on `/api/health` so Jamai gets a page
  if staging goes down during their workday.

### Carryover from Phase 1A punch list

These were descoped from Phase 1A but block a polished customer demo:

- **5b. Tenant settings editor** — make `/settings` editable (business
  name, address, currency, VAT rate, prefix, next number, logo upload).
- **6. Edit + delete invoice UI** — API routes exist
  (`PUT`/`DELETE /api/invoices/:id`); dashboard doesn't expose them yet.
- **7. Customer-name + date-range filters** on the invoices list
  (Phase 1A ships the most recent 25 unfiltered).
- **8. Sentry + Plausible** — wizard-install, document DSN/domain in env.
- **9. Per-tenant numbering load test** — 10 concurrent
  `POST /api/invoices` against the same tenant; confirm no duplicate
  numbers under contention before the first customer sees it.

## Decisions made along the way (no human input needed)

- **Auth.js v5 over v4.** Spec says "NextAuth"; v5 is the current
  stable line with cleaner App Router integration. No functional
  difference for the magic-link flow.
- **Database sessions, not JWT.** Required because the
  `session` callback reads `tenantId` off the User row at request time;
  JWT sessions would mean stale tenant data after settings edits.
- **`prisma migrate deploy` (not `db push`) for the first deploy.**
  Initial migration is committed at
  `prisma/migrations/20260517000000_init/migration.sql`, generated via
  `prisma migrate diff --from-empty --to-schema-datamodel`. Build runs
  `migrate deploy` which is idempotent — safe to re-run on any deploy.
- **Marketing page is minimal.** One headline, one CTA, one footer.
  Brand-correct but not "Lighthouse 90+ marketing-grade" yet — that's
  a Phase 1B polish pass when we know the live URL and can run the
  audit.
- **Settings page is read-only.** Phase 1A had "minimal create-invoice
  flow" as the UI goal; the editable settings form is Phase 1B (item 5).
- **No tenant invite UI.** Spec explicitly defers that to Phase 1B.
- **PDF design is a faithful re-creation of the V.1 layout from spec
  description.** The `reference-current-app/` folder mentioned in the
  kickoff prompt wasn't committed to the repo, so I mirrored the layout
  from the prose description in `01-PROJECT-SPEC.md` and `04-USER-FLOWS.md`
  (lavender header → green-purple-green strip → items table → totals →
  "Powered by AI Solutions Barbados" footer). If the V.1 HTML/PDF lands
  in the repo later, I'll cross-check pixel-for-pixel.

## File map of what was added today

```
README.md                               (rewritten — was 3-line stub)
DEPLOY-CHECKLIST.md                     (new — Render click-through guide)
render.yaml                             (new — Render Blueprint)
package.json, tsconfig.json,            (new — Next 14 + TS strict)
  next.config.mjs, tailwind.config.ts,
  postcss.config.mjs, .gitignore,
  .env.example
prisma/
  schema.prisma                         (matches docs/03-DATA-MODEL.md)
  seed.ts                               (1 demo tenant + 3 invoices)
src/
  app/
    layout.tsx, page.tsx                (marketing landing)
    signin/page.tsx                     (magic-link request)
    signin/check-email/page.tsx
    (dashboard)/layout.tsx              (auth-gated, trial banner)
    (dashboard)/invoices/page.tsx       (history list)
    (dashboard)/invoices/new/page.tsx
    (dashboard)/settings/page.tsx       (read-only Phase 1A)
    api/auth/[...nextauth]/route.ts
    api/health/route.ts
    api/invoices/route.ts               (POST create, GET list)
    api/invoices/[id]/route.ts          (GET, soft DELETE)
    api/invoices/[id]/pdf/route.ts      (server-side PDF stream)
  components/NewInvoiceForm.tsx         (client form, live totals)
  lib/
    db.ts                               (Prisma singleton)
    auth.ts                             (Auth.js v5 + Prisma adapter + tenant creation)
    auth-handlers.ts                    (handlers split for route file)
    email.ts                            (console-log Phase 1A → SendGrid Phase 1B)
    tenant.ts                           (withTenant wrapper — NON-NEGOTIABLE)
    validation.ts                       (Zod + computeTotals)
    pdf.ts                              (jsPDF + autoTable, brand layout)
  styles/globals.css                    (Tailwind + brand utility classes)
  types/next-auth.d.ts                  (Session.user.tenantId / role)
docs/STATUS-DAY-1.md                    (this file)
```

Roughly 1,200 lines of TypeScript + Prisma + Tailwind. No `any` in
tenant-scoped paths. Zero TS errors. Clean production build.
